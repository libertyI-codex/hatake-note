# 畑ノート Ver.2.0 試作2

家庭菜園の4×4の畑マスと、各マスをいつからいつまで何の作物に使うかを管理する、スマホ優先のローカルWebアプリです。

## 作り直した理由

Ver.1系では、区画、作業記録、写真、育成スケジュール、栽培計画など複数の概念を扱っていました。Ver.2.0では日常操作を簡単にするため、管理対象を次の2つに絞りました。

- 4×4の畑マス
- 使用マス、作物名、開始日、終了予定日を持つ作付け予定

Ver.1系のコードや保存データは使用せず、別アプリとして新規構築しています。

## 基本思想

「現況」と「予定」を別データとして保存しません。すべての作付けに開始日と終了予定日を持たせ、今日の日付との関係から状態を自動判定します。

- 開始日前：今後の予定
- 開始日から終了予定日まで：栽培中
- 終了予定日後：終了済み

状態のように日付から計算できる値は保存しません。

## 画面構成

画面と下部ナビは次の3つだけです。

1. 畑
2. 年間予定
3. データ

### 畑

- 1〜16番の4×4畑図
- 常に今日栽培中の作物を表示
- 現在空いているマスには、開始日が最も近い次作予定を1件表示
- 現在も次作予定もないマスは「空き」と表示
- 番号は縦方向へ増える順（1・5・9・13、2・6・10・14…）で配置
- 作付けの新規登録、詳細確認、編集、削除

初回起動時はサンプルを登録せず、16マスすべて「空き」と表示します。

### 複数マス選択

新しい作付けでは畑図から1つ以上のマスを選びます。選択画面も番号が縦方向へ増える配置です。たとえば2・6・10を選んで「じゃがいも」を登録すると、3マスが同じ色と作物名で表示されます。

同じ作付けに属する隣接マスは内側の境界を薄く表示します。保存される `cellIds` の番号やデータ構造は変更しません。

### 作付け登録・編集・削除

保存項目は次のとおりです。

- 使用するマス
- 作物名
- 開始日
- 終了予定日
- メモ（任意）

編集ではすべての項目を変更できます。終了予定日は開始日以降である必要があります。削除前には確認ダイアログが表示されます。

### 期間重複チェック

次の両方に該当する作付けは保存できません。

- 1つ以上同じマスを使う
- 開始日から終了予定日までの期間が重なる

開始日と終了予定日を含めて重複判定します。編集時は編集中の作付け自身を判定対象から除外します。

### 年間予定

選択した年について、マス1〜16を必ず16行で表示します。

- 左側：マス番号
- 右側：1月〜12月
- 各月に該当する作物名を色付きラベルで表示
- 複数マスの作付けも、それぞれのマスの行へ個別表示
- 同じ月に作物が入れ替わる場合は、複数ラベルを開始日順に表示
- 前年から継続する作付け、翌年まで続く作付けにも対応
- スマホでは横スクロール可能
- マス番号列を横スクロール時に固定
- 作物ラベルをタップして詳細を表示

行の並び順はマス1〜16の数値順です。

### データ

- JSONバックアップの書き出し
- JSONバックアップの読み込み
- 全作付けデータの削除

復元前にファイルを検証し、読み込み件数を表示します。確認後、現在の作付けデータをバックアップ内容で上書きします。不正なファイルでは現在データを変更しません。

バックアップファイル名は `hatake-note-v2-backup-YYYYMMDD-HHMM.json` です。

## データ構造

```json
{
  "id": "crop-plan-xxxxxxxx",
  "cellIds": [2, 6, 10],
  "cropName": "じゃがいも",
  "startDate": "2026-09-01",
  "endDate": "2026-12-15",
  "memo": "秋じゃが",
  "createdAt": "2026-07-26T00:00:00.000Z",
  "updatedAt": "2026-07-26T00:00:00.000Z"
}
```

`cellIds` は1〜16の重複しない整数配列です。

## 保存仕様

作付けデータはブラウザのlocalStorageへ保存します。

保存キー：

```text
hatakeNoteV2.cropPlans.v1
```

Ver.1系の保存キーやIndexedDBにはアクセスしません。Ver.1系とVer.2.0のデータは完全に別です。

畑画面は表示日を持たず、常に端末の今日の日付を基準に表示します。

## ファイル構成

```text
hatake-note-v2/
  index.html
  style.css
  data.js
  app.js
  README.md
```

React、外部ライブラリ、CDN、Firebase、Service Workerは使用していません。

## 起動方法

`index.html` をブラウザで開くと使用できます。ブラウザの保存制限を避けて確認する場合は、このフォルダを簡易Webサーバーで配信してください。

例：

```powershell
python -m http.server 8000
```

その後、ブラウザで `http://localhost:8000/` を開きます。

静的ファイルだけで構成しているため、GitHub Pagesでも公開できます。今回、GitHub Pagesへのアップロードは行っていません。

CSSとJavaScriptの読み込みには次のキャッシュ対策クエリを付けています。

- `style.css?v=20p2`
- `data.js?v=20p2`
- `app.js?v=20p2`

## 注意点

- データは同じ端末、同じブラウザ内だけに保存されます。
- ブラウザデータを削除すると作付けデータも消えます。
- 機種変更や別端末への移行にはJSONバックアップを使用してください。
- PCとスマホの自動同期はありません。

## 今回未実装の機能

- 作業記録
- 写真記録
- 育成スケジュール
- 撤去期限の自動計算
- 準備期間
- 植え方
- 通知
- 天気
- AIアドバイス
- 収穫量記録・グラフ
- 区画一覧・区画名管理
- 4×4以外の行列数
- ドラッグ・ピンチ操作
- 完全な図形描画
- クラウド同期
- ログイン
- 夫婦共有
- Firebase
- CSV・PDF出力
- Ver.1系データの自動移行
- GitHub Pagesへのアップロード

## Ver.2.0 試作2の変更点

- 畑画面の表示日機能を廃止しました。
- 畑画面は常に今日の現況を表示します。
- 現在空いているマスには、開始日が最も近い次作予定を表示します。
- 畑図の番号を縦方向へ増える配置に変更しました。
- 表示順は1・5・9・13、2・6・10・14、3・7・11・15、4・8・12・16です。
- 保存される `cellIds` 自体は変更していません。
- 年間予定表をマス1〜16の16行表示へ変更しました。
- 複数マスの作付けも各マスの行へ個別表示します。
- 同じ月に複数作付けがある場合は複数ラベルで表示します。
- 保存キー `hatakeNoteV2.cropPlans.v1` とデータ構造は変更していません。
- キャッシュ対策クエリは `v=20p2` です。
- Service Workerは使用していません。

## 保存記録：Ver.2.0 試作2

- 畑ノート Ver.2.0 試作2として保存した版です。
- 畑画面の表示日機能を廃止しています。
- 畑画面は今日の現況を表示します。
- 空きマスには、開始日が最も近い次作予定を表示します。
- 区画番号を縦方向へ増える配置に変更しています。
- 表示順は1・5・9・13、2・6・10・14、3・7・11・15、4・8・12・16です。
- 保存される `cellIds` 自体は変更していません。
- 年間予定表はマス1〜16の16行表示です。
- 複数マス作付けも各マスの行へ個別表示します。
- 同じ月の複数作物を複数ラベルで表示できます。
- 保存キーは引き続き `hatakeNoteV2.cropPlans.v1` です。
- データ構造は変更していません。
- Service Workerは使用していません。
- キャッシュ対策クエリは `v=20p2` です。
